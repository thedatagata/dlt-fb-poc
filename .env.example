NEUROGRAPH_CORE_PG_HOST=cloud-sql-proxy
NEUROGRAPH_CORE_PG_DATABASE=core
NEUROGRAPH_CORE_PG_USER=staginguser
NEUROGRAPH_CORE_PG_PASSWORD="{replace with your password}"

GOOGLE_APPLICATION_CREDENTIALS="{replace with your path to the Google Application Credentials file}"
GCS_CREDS_PATH="{replace with your path to the Google Application Credentials file}"
GCS_BUCKET_NAME=dlt_lakehouse_poc
GCP_PROJECT_NAME=axon-platform-dev

# Cloud SQL instance connection name for the cloud-sql-proxy docker
# service (only used by the Phase D DuckDB→Postgres demo notebook).
# Format: <project>:<region>:<instance>
CLOUD_SQL_INSTANCE_CONNECTION_NAME="{replace with staging Cloud SQL connection name}"