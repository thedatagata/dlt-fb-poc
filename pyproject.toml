[project]
name = "dlt-sandbox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dlt[duckdb,workspace]>=1.25.0",
    "polars>=1.40.1",
    "pyarrow>=24.0.0",
]
